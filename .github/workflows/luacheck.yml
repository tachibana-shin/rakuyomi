name: luacheck

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
  pull_request:
    branches: [main]
    paths:
      - 'frontend/**'
  workflow_call:

jobs:
  luacheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Lua and luacheck
        run: |
          sudo apt-get update
          sudo apt-get install -y lua5.1 luajit luarocks
          sudo luarocks install luacheck

      - name: Run luacheck
        run: luacheck frontend/

      - name: Test chapter language refresh and selection
        run: luajit frontend/tests/chapter_languages.lua
